# Backend

Backend written in rust using the axum framework.

### Setup

To run the backend, you will need to have Rust installed. You can install Rust using [rustup](https://rustup.rs/).

Then you can run the backend using the following command:

```bash
cargo run
```